<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- ============================================== -->
    <!-- 1. BLUETOOTH & MESH NETWORKING PERMISSIONS     -->
    <!-- ============================================== -->
    <!-- Legacy Bluetooth (Android 11 and below) -->
    <uses-permission android:name="android.permission.BLUETOOTH" />
    <uses-permission android:name="android.permission.BLUETOOTH_ADMIN" />

    <!-- Modern Bluetooth (Android 12+ requires explicit granular permissions) -->
    <uses-permission android:name="android.permission.BLUETOOTH_ADVERTISE" />
    <uses-permission android:name="android.permission.BLUETOOTH_SCAN" />
    <uses-permission android:name="android.permission.BLUETOOTH_CONNECT" />

    <!-- ============================================== -->
    <!-- 2. WI-FI DIRECT / NETWORK PERMISSIONS          -->
    <!-- ============================================== -->
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />
    <uses-permission android:name="android.permission.CHANGE_WIFI_STATE" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <!-- Android 13+ requires this for Wi-Fi Direct peer discovery -->
    <uses-permission android:name="android.permission.NEARBY_WIFI_DEVICES" />

    <!-- ============================================== -->
    <!-- 3. LOCATION PERMISSIONS                        -->
    <!-- ============================================== -->
    <!-- Android heavily enforces location permissions because scanning for
         BLE/Wi-Fi tags can theoretically be used to track a user's location -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />

    <!-- ============================================== -->
    <!-- 4. BACKGROUND EXECUTION                        -->
    <!-- ============================================== -->
    <!-- Needed to provide haptic feedback when SOS triggers -->
    <uses-permission android:name="android.permission.VIBRATE" />
    <!-- Keeps the CPU awake while the phone screen is off during an emergency -->
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <!-- Allows our mesh router and SOS coordinator to run as persistent background services -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <!-- Required for Android 14+ foreground services of type connectedDevice -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_CONNECTED_DEVICE" />

    <!-- ============================================== -->
    <!-- 5. HARDWARE FEATURES                           -->
    <!-- ============================================== -->
    <uses-feature
        android:name="android.hardware.bluetooth_le"
        android:required="true" />

    <uses-feature
        android:name="android.hardware.wifi.direct"
        android:required="true" />

    <uses-feature
        android:name="android.hardware.sensor.accelerometer"
        android:required="true" />

    <!-- ============================================== -->
    <!-- APPLICATION                                    -->
    <!-- ============================================== -->

    <application
        android:allowBackup="true"
        android:fullBackupContent="@xml/backup_rules"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AegisMesh">

        <!-- Real entry point: auth first, then Home/Profile route from there. -->
        <activity
            android:name=".activities.LoginActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".activities.HomeActivity" />
        <activity android:name=".activities.ProfileActivity" />
        <activity android:name=".activities.EmergencyActivity" />
        <activity android:name=".activities.IdVerificationActivity" />
        <activity android:name=".activities.SelfieVerificationActivity" />

        <!-- SOS Coordinator Foreground Service -->
        <service
            android:name=".services.SOSService"
            android:enabled="true"
            android:exported="false"
            android:foregroundServiceType="connectedDevice" />

        <!-- GPS Location Service, bound by SOSService -->
        <service
            android:name=".services.LocationService"
            android:enabled="true"
            android:exported="false" />

        <!-- Mesh Networking Foreground Service -->
        <service
            android:name=".services.MeshService"
            android:enabled="true"
            android:exported="false"
            android:foregroundServiceType="connectedDevice" />

    </application>

</manifest>
